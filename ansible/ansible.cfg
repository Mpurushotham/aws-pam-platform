[defaults]
inventory = inventory/aws_ec2.yml
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
interpreter_python = auto_silent
roles_path = roles

# Connect over SSM Session Manager — no SSH keys, fully recorded.
[ssh_connection]
pipelining = True
